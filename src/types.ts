export enum Handedness {
    Unknown,
    Left,
    Right
}

export enum Finger {
    Thumb,
    Index,
    Middle,
    Ring,
    Pinky
}

export enum Knucle {
    Proximal,
    Intermediate,
    Distal
}
